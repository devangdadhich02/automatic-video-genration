# Rename to: backend/.env
# Windows (PowerShell) note: paths can be like C:\\path\\to\\file.json

# General
ENV=dev

# LLM (OpenAI)
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_TIMEOUT_SECONDS=60
SCRIPT_JOB_MAX_SECONDS=900

# Optional: OpenAI-compatible base URL (leave empty for OpenAI)
OPENAI_BASE_URL=

# Optional: OpenRouter (for Claude/Gemini/other models via OpenAI-compatible API)
OPENROUTER_API_KEY=or-your-openrouter-key-here

# Vector DB (RAG)
# If deps are not installed, backend still runs but RAG retrieval returns empty.
VECTOR_DB_PROVIDER=chroma

# B-roll sources (optional)
PIXABAY_API_KEY=your-pixabay-key
PEXELS_API_KEY=your-pexels-key

# FFmpeg
FFMPEG_PATH=ffmpeg

# Supabase (optional; used by the web dashboard)
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR_ANON_KEY

# Google Sheets (optional; if not set, local SQLite is used)
GOOGLE_SHEETS_ID=your_google_sheet_id_here

# Service account JSON (either a file path OR the raw JSON string)
# Option A (recommended): file path
GOOGLE_SERVICE_ACCOUNT_JSON=C:\\path\\to\\service-account.json
# Option B: raw JSON (single line)
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...",...}
